users(uid, name, city, crdate)
badges(bname, type)
ubadges(uid, bdate, bname)
posts(pid, pdate, title, body, poster)
tags (pid, tag)
votes(pid, vno, vdate, uid)
questions(pid, theaid)
answers(pid, qid)

5+1+1+1
boring: 31(24), 34 (22)
extra tag: 31
fun: 31, 32(23), 33(22)

select votes.pid, count(votes.vno), u